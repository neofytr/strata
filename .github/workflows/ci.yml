name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.cc }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cc: [gcc, clang]
    env:
      CC: ${{ matrix.cc }}
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.cc }} pkg-config zlib1g-dev

      - name: Compile strata object
        run: |
          $CC -c buildsysdep/neobuild.c -o buildsysdep/neobuild.o \
            -Wall -Wextra -Werror -std=c11 \
            -D_POSIX_C_SOURCE=200809L -D_GNU_SOURCE

      - name: Run test suite
        run: bash tests/run_tests.sh

  build-macos:
    name: Build & Test (macOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: brew install pkg-config zlib

      - name: Compile strata object
        run: |
          cc -c buildsysdep/neobuild.c -o buildsysdep/neobuild.o \
            -Wall -Wextra -std=c11 \
            -D_POSIX_C_SOURCE=200809L -D_GNU_SOURCE

      - name: Run test suite
        continue-on-error: true
        run: bash tests/run_tests.sh

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc cppcheck

      - name: Run cppcheck
        run: |
          cppcheck --enable=warning,style,performance \
            --suppress=missingIncludeSystem \
            --error-exitcode=1 \
            buildsysdep/neobuild.c || true

      - name: Verify -Wall -Wextra -Werror clean build
        run: |
          gcc -c buildsysdep/neobuild.c -o buildsysdep/neobuild.o \
            -Wall -Wextra -Werror -std=c11 \
            -D_POSIX_C_SOURCE=200809L -D_GNU_SOURCE

  docs:
    name: Deploy Docs
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [build-and-test, lint]
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
